if a table is memberRedefinable allow redef["var"] = 1 to use aliases
add check to see that alias isn't redefining global variable
optimize to call function without () 
Function scope should store owner (it's own symbol)
Improve inCodeSpace check